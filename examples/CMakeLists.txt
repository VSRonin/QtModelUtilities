if(BUILD_MODELSERIALISATION)
    add_subdirectory(exem_ModelSerialisation)
endif()
if(BUILD_INSERTPROXY)
    add_subdirectory(exem_InsertProxyModel)
endif()
if(BUILD_TRANSPOSEPROXY)
    add_subdirectory(exem_TransposeProxyModel)
endif()