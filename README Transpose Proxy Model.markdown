# Transpose Proxy Model

This proxy will transpose the cource model. Rows of the original model will become columns in the proxy. 
If your model is a tree, the parents will be inverted as well.

### Dependencies

+ Qt Core